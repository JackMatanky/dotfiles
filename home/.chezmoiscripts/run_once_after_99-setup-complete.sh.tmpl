#!/usr/bin/env bash
# Mark setup as complete
set -euo pipefail

{{ template "logging.sh" . }}

# Ensure marker file exists
mkdir -p "$HOME/.config"
touch "$HOME/.config/chezmoi-initial-setup-complete"

log_success "Setup complete - restart your shell to apply changes"
